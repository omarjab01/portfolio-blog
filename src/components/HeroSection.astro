---

---

<div class="flex flex-col h-[90vh] container mx-auto p-6 pb-0 max-w-7xl">
    <div class="flex flex-col gap-4 items-center mt-12">
        <h1 class="text-3xl font-semibold text-center">Progettazione e creazione di siti web moderni e veloci</h1>
        <p class="text-center text-sm text-slate-500">Realizzo sia siti wordpress che siti web con framework più recenti quali next js ed astro, specialmente quest’ultimo.</p>
        <div class="flex flex-col gap-1 mt-4 items-center justify-center">
            <button class="bg-slate-950 text-white p-4 rounded-xl w-fit text-sm">Parlami del tuo progetto</button>
            <button class="p-4 rounded-xl w-fit text-sm text-center">Segui il mio blog</button>
        </div>
    </div>
    <div class="w-full img-container flex-1">
        <img src="/mockup.png" class="img-mockup"/>
        <img src="/bg_blur.png" class="img-bg-blur"/>
    </div>
</div>


<style>
    .img-container{
        max-width: 100%;
        max-height: 100%;
        overflow: hidden;
        position: relative;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .img-container .img-mockup{
        position: absolute;
        max-width: 100%;
        max-height: 100%;
        object-fit: contain;
        transform: rotate(10deg);
        z-index: 2;
        scale: 1.8;
    }

    .img-container .img-bg-blur{
        transform: rotate(-25deg);
    }
</style>